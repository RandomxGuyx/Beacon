{
  "name": "beacon",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node dev.js",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
